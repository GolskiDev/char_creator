import 'package:flutter/material.dart';

class SpellFilterDrawer extends StatelessWidget {
  const SpellFilterDrawer({super.key,});

  @override
  Widget build(BuildContext context) {
    return Drawer(
      child: Container(),
    );
  }
}