name: spells_and_tools_texts
description: "A Flutter package for generating and reviewing LLM-powered texts for spell images."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.4
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  langchain: ^0.8.1
  langchain_anthropic: ^0.3.1
  langchain_ollama: ^0.4.1
  langchain_openai: ^0.8.1+1
  path_provider: ^2.1.5
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # Pure Dart/Flutter package — no native plugin code needed.
